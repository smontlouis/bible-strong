# Assistant API contract

Public request/event types, validation and SSE decoding used by the application. No provider SDK, server implementation, prompts or private repository dependency. The independently deployed service owns execution. Matching files are mirrored in the server repository; synchronize API changes on both sides before deployment.
