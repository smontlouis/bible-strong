import React from 'react'

import Box from '~common/ui/Box'
import Text from '~common/ui/Text'
import { ReadingSlice as ReadingSliceProps } from '~common/types'

const ReadingSlice = ({ id, description, slices }: ReadingSliceProps) => (
  <Box>
    <Text>Coucou</Text>
  </Box>
)

export default ReadingSlice
