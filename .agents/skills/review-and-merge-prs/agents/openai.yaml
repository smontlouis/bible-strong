interface:
  display_name: "Review And Merge PRs"
  short_description: "Process PRs one by one with a test gate"
  default_prompt: "Use $review-and-merge-prs to process open PRs one by one, prepare each for simulator testing, then wait for my approval before merging."
policy:
  allow_implicit_invocation: true
