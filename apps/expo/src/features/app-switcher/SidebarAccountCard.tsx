// The account footer belongs to the Web sidebar; native keeps its existing settings entry.
export default function SidebarAccountCard(_props: {
  openSettings: () => void
  onSelectContent?: () => void
}) {
  return null
}
